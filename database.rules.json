{
  "rules": {
    "chats": {
      "$chatId": {
        ".read":  "auth != null",
        ".write": "auth != null",
        "messages": {
          "$msgId": {
            ".validate": "newData.hasChildren(['text', 'senderRole', 'time'])"
          }
        }
      }
    }
  }
}
